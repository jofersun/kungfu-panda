const setPageTitle = (title: string) => ({
  type: "SET_PAGE_TITLE",
  payload: title,
});

// const setPageTitle = (payload) => ({
//   type: "SET_PAGE_TITLE",
//   payload: payload,
// });

export default setPageTitle;
